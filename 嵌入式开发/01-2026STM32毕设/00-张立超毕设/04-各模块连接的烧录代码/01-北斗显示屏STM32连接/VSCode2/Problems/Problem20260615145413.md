## 任务目标
将 OLED 最终界面固定显示的经纬度改为：

- `LAT:36.157`
- `LON:120.374`

## 思考过程
当前显示逻辑已经固定在 `Core/Src/bds_display.c` 的两个字符串常量中。用户只要求修改显示数值，因此不改启动提示、中文标题、OLED 字模、串口解析或烧录配置。

## 遇到的问题
暂无阻塞。当前旧显示值为 `LAT:36` 和 `LON:120`。

## 解决方法
修改 `Core/Src/bds_display.c` 中两个固定显示常量：

- `LAT:36` 改为 `LAT:36.157`
- `LON:120` 改为 `LON:120.374`

## 验证结果
执行 `cmake --build --preset Debug`，编译和链接成功。资源占用：

- RAM: `1856 B / 20 KB`，`9.06%`
- FLASH: `12068 B / 64 KB`，`18.41%`

执行 STM32CubeProgrammer 烧录，写入和校验均成功，并已执行软件复位。

## 代码变更摘要
- `Core/Src/bds_display.c`：将固定显示的经纬度字符串改为 `LAT:36.157` 和 `LON:120.374`。
- `Problems/Problem20260615145413.md`：记录本次修改和验证结果。

## 任务目标
撤销上一轮为规避 Cortex-Debug 中文路径问题而做的英文路径配置更改，然后再次编译并烧录当前固件。

## 思考过程
用户要求“撤销上面的更改，然后再次烧录”。这里的“上面的更改”按上下文理解为上一轮排查调试问题时创建的 `F:\stm32_bds_vscode2` 英文目录联接，以及 `.vscode/tasks.json`、`.vscode/launch.json` 中改为英文路径的配置。

本次不撤销此前的业务显示代码，例如固定显示 `LAT:36.157` 和 `LON:120.374`。

## 遇到的问题
当前 `.vscode/tasks.json` 和 `.vscode/launch.json` 仍指向 `F:\stm32_bds_vscode2`，且该路径是 junction。需要恢复为工程内原来的 `${workspaceFolder}`、`${workspaceRoot}` 和相对 ELF 路径。

## 解决方法
已执行：

1. 恢复 `.vscode/tasks.json` 中的任务工作目录和烧录 ELF 路径。
2. 恢复 `.vscode/launch.json` 中的调试工作目录、ELF 路径和 `openocd.cfg` 路径。
3. 删除 `F:\stm32_bds_vscode2` junction。
4. 清理并重新生成 `build/Debug`，避免 CMake 缓存继续指向英文路径。
5. 编译并烧录。

## 验证结果
`F:\stm32_bds_vscode2` junction 已删除。

执行 `cmake --preset Debug` 和 `cmake --build --preset Debug` 成功。资源占用：

- RAM: `1856 B / 20 KB`，`9.06%`
- FLASH: `12068 B / 64 KB`，`18.41%`

重新检查 `build/Debug/CMakeCache.txt`，`CMAKE_HOME_DIRECTORY`、`VSCode_SOURCE_DIR`、`VSCode_BINARY_DIR`、`CMAKE_TOOLCHAIN_FILE` 均已恢复为当前原工程路径。

执行 STM32CubeProgrammer 烧录，写入和校验均成功，并已执行软件复位。

配置恢复后仍保留当前业务固件内容，即最终界面固定显示 `LAT:36.157` 和 `LON:120.374`。

## 代码变更摘要
- `.vscode/tasks.json`：恢复为 `${workspaceFolder}` 工作目录和 `${workspaceFolder}\build\Debug\VSCode.elf` 烧录路径。
- `.vscode/launch.json`：恢复为 `${workspaceRoot}`、`./build/Debug/VSCode.elf` 和 `openocd.cfg`。
- `build/Debug`：删除旧缓存后从当前原工程路径重新生成。
- `Problems/Problem20260615150549.md`：记录本次撤销、重建和烧录验证。
